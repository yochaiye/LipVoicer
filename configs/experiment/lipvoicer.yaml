# @package _global_
defaults:
  - /model: wavenet
  - /dataset: lipvoicer

diffusion:
  T: 400
  beta_0: 0.0001
  beta_T: 0.02
  beta: null

model:
  mel_upsample: [2, 2] # Product should equal hop size in the dataset config!
